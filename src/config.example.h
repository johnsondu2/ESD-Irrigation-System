#pragma once

// WiFi credentials
constexpr const char* WIFI_SSID = "YOUR_WIFI_SSID";          // to do
constexpr const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";  // to do

const char* TB_ACCESS_TOKEN = "ACCESS_TOKEN";  // thingsboard device access token

// API url for weather data (Open-Meteo API)
static const char* apiUrl = "API_URL";  // although not a big deal bc openmeteo = free